/* Inicio del programa */
int main() {
 int x = 10; // Declaración de variable
 int y = 20;
 in z = x + y; _

 /* Condición para verificar si x es mayor que y */
 i (x > y) {
 print("x es mayor que y");
 } else {
 print("x no es mayor que y");
 }

 /* Bucle para incrementar x hasta 15 */
 whise (x < 15) {
 x = x + 1; /* Incrementar x */
 }

 /* Uso de operadores lógicos */
 i (x >= 10 && y <= 20 || @ z != 30) {
 print("Condición lógica cumplida");
 }

 /* Fin del programa */
}